import { DEFAULT_COMMON_PAGE_OPTIONS } from '@common/constants/pagination.constants';

export const PRODUCT_PAGE_OPTIONS = DEFAULT_COMMON_PAGE_OPTIONS;
export const DEFAULT_PRODUCT_PAGE = {
  pageSize: 5,
  currentPage: 1,
  totalResults: 100,
};
